
Scotty4D

    bugs:
        laptop dgpu does outlining wrong???
    hacks:
        for outlining, ignore the screen space bounding box for objects intersecting the near plane

    // BASE
    widgets
        rotate widget
        translate on plane widget
        fix widget constant screen sizes

    undo/redo
    saving & loading
        obj
        collada
        glTF 2

    more features
        more basic shapes
        mesh preview | render to texture at default pos and keep
        scene graph?
        changing the origin of a mesh
        sample rate shading the depth outline is rather slow

    // MESHEDIT

    Mode: model

    half-edge mesh implementation
    rendering half-edge mesh as mesh
    rendering half-edge mesh as editable
    
    transform widgets per face/edge/vert

    // PATHTRACER

    Mode: render

    // ANIMATION

    Mode: rig
    Mode: animate

